tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/ssl/traefik/local-cert.pem
        keyFile: /etc/ssl/traefik/local-key.pem
  certificates:
    - certFile: /etc/ssl/traefik/local-cert.pem
      keyFile: /etc/ssl/traefik/local-key.pem
